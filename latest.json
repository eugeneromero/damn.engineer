---
layout: null
---

{
    {% assign post = site.posts.first %}
    "title": {{ post.title | jsonify }},
    "url": "{{ post.url | absolute_url }}",
    "filename": "{{ post.relative_path }}",
    "medium": "{{ post.publish-to-medium }}"
}
